# Here the change is that there are obstacle in the path. So we can not choose the path with obstacle.

# so we can add just one another base case 
#  if(i>=0 and j>=0 and matrix[i][j]==-1):
        # return 0

# Everything else will remain same.